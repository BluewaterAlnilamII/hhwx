# 本地组曲回归资料

英文版本：[bandori-medley-fixtures.md](bandori-medley-fixtures.md)

## 用途与边界

保留 main/dev 的历史结果，用来核对独立新计算器，不引入旧搜索架构。资料包括原始 HHWX 档案、缓存的主数据／谱面／活动数据、选出的原始报告和索引。整理脚本不运行旧求解器，也不启动新搜索。

真实档案、原始档案集合、报告、本机路径和生成的索引全部放在已被 Git 忽略的 `temp/medley-regression-fixtures/`，不得提交或公开。Git 只保存说明和测试工具；不增加依赖，不改应用代码。

首次收集包含 21 份不同的国服档案：119／1329／1889 卡的早期档案，以及两批各十份、其中两份重复的真实样本。这些都是完整卡池，119 张卡也不属于可以轻易穷举的极小输入。

## 整理与核验

用现成的私有档案整理包作为 `--seed`，分别传入保存历史报告的目录：

```sh
node --import tsx scripts/archive-bandori-medley-fixtures.mjs --seed <私有档案包> --source main=<报告目录> --source dev=<报告目录>
node --import tsx scripts/archive-bandori-medley-fixtures.mjs --verify
```

档案包采用现有的 `manifest.json` 格式，含档案、原始集合、数据和 `reference-only/` 报告。脚本保留其原文件，再读取已知的组曲基准、活动矩阵、独立进程测试和 low-memory HHWX 报告。不收集日志、二进制、转换成 Calc 格式的输入或求解器代码。再次传入相同来源可更新索引，已经复制的证据文件不会被删除。

本地 `README.md` 列出档案；`manifest.json` 记录：

- 档案内容校验值和带日期的别名；账号标识只能定位候选档案，报告记录的卡数不一致时不建立索引；
- 各来源目录的数据快照，相同文件只保存一份；
- 原报告路径、日期、输入设置、分数，以及结果在报告中的位置；
- 是否保存了卡牌、道具 ID 和明确的队长；
- 哪些来源缺少缓存、哪些匿名结果未找到对应原档案。

每个来源／档案／歌曲／设置组合分别保留：最高报告分数、带卡牌和道具的最高分数、另有明确队长的最高分数。原整理包中的参考报告也保留；同一个文件被多项选中时不重复复制。核验覆盖文件校验值、实际 HHWX 档案解码和索引引用。

## 分数比较规则

1. 日常回归直接运行新搜索，与留存的历史平均总分比较，保持档案、歌曲顺序、难度、PERFECT 比例、活动设置及数据一致。不把运行旧求解器、复算旧队伍或向搜索提供旧答案作为前置步骤。旧队伍复算只留给之后确有需要的专项差异排查。
2. 对相同的完整输入，新搜索达到或超过历史平均总分，分数回归检查即通过。最优性证明另行记录：未完成的结果不能证明最优，但已经达到参考分数后，不再算分数回归失败。单纯更高不能证明计分正确，因为多算分也会更高。
3. 原样保留 `score`、`averageScore` 以及旧报告的精确／完成标记，不擅自混同分数字段或把旧标记当成新证明。报告附带的候选队伍不一定就是报告中的第一名。
4. 数据快照只表示来源目录里实际保存的文件。缺少每次运行的数据校验值或提交号时，不能声称已确认那次运行用的版本；目录标签也不等于生成报告时的分支名。
5. 真实样本测试脚本固定 expert、全 PERFECT、无 fever；早期通用基准报告没有记录可配置的 PERFECT 比例，索引保留未知。比较时如作假设，应明确记录，不能悄悄补成历史事实。旧队伍缺队长时，不从卡牌顺序猜，也不因此阻止直接比较总分。
6. 从档案取十五张卡的小样本与原始完整卡池是两种搜索输入，不能直接比较两者的最优分数。

## 直接比较入口

现有脚本从归档 HHWX 档案和原始数据重新生成输入，构建原生 release 程序，直接比较新搜索与历史平均分。采用 main 目录留存的数据快照，不调用旧搜索。119 卡使用 expert 歌曲 `295 → 300 → 703`；961／962／972 卡使用 expert 歌曲 `385 → 193 → 619`；均为全 PERFECT、无 fever、完整已拥有道具范围。

重跑四份档案的十四个场景，复用两条现有命令即可：

```sh
node --import tsx scripts/compare-bandori-medley-search.mjs --case 119-no-event
node --import tsx scripts/compare-bandori-medley-search.mjs --remaining
```

不传选项时运行 119 卡两例。`--remaining` 运行其余十三例，单例失败后继续；`--six` 只运行 119／961 卡六例。已有 `--diagnose` 模式见[搜索说明](bandori-medley-search.zh-CN.md)。

每例仍限时 300 秒、搜索存储预算 256 MiB，逐例执行。Windows 每秒采样原生进程峰值工作集，达到 1 GiB 时停止，外部截止时间为 315 秒。这是原生进程采样限制，不是操作系统硬性配额，也不是浏览器／WASM 增量内存。失败后不自动提高预算或改算法。

较大档案读取已知活动矩阵格式的 `rows[n].all.score`，该字段保存组曲主要平均分。保留实际字段和旧 exact 标记，不用附带候选的分数代替主要结果。119 卡报告仍缺历史 PERFECT 比例，所有历史报告仍缺已核实的当次数据哈希；新运行完整记录自己的输入、哈希、设置、提交、结果、耗时和内存。

## 小卡池阶段验收

2026-08-31，在干净代码提交 `eb66f9b` 上，按潜能与角色任务合并取整的新规则完成全部十四场搜索：十三场与历史平均分相同；972 卡活动 244 为 8,594,227，比历史 8,591,251 高 2,976。这是最终版本完整重跑，不再只是复算已保存队伍。

原生搜索耗时合计 159.922 秒，最慢单例 35.232 秒；采样到的原生进程峰值工作集为 29.30 MiB，预算内搜索存储峰值为 20.34 MiB。119 卡两例在首次采样前结束，内存记为未采到，不能记为零。原始档案、主数据、谱面、设置和历史参考均与上一轮相同；规范化输入的变化仅在已批准取整规则影响的角色／活动参数。

完整结果和核对记录保存在私有目录 `runs/2026-08-31-final-low-pressure/`，对应原始运行 `2026-08-31T14-48-26.892Z`、`2026-08-31T14-48-29.719Z`。972 卡活动 244 的高分来源已在专项排查中解释；本轮未修改任何新旧算法。验收只覆盖这十四场。

## 四场均已完成档案阶段

按整份档案筛选：超过 1,000 张卡，且无活动、活动 244、260、323 四场均有完整道具范围 exact 留存结果。本轮批准的档案是 **1036、1039、1161、1211、1229、1252、1318、1425、1433、1513、1522、1703 卡**，共十二份，每份四场，合计 **48 场**。

```sh
node --import tsx scripts/compare-bandori-medley-search.mjs --completed-profiles
```

复用现有测试入口、输入生成和资源记录。卡数只作为便于阅读的样本标签，比较以每次运行记录的不可变档案内容哈希为准。全部采用 main 目录留存数据、expert 歌曲 385／193／619、全 PERFECT、无 fever、完整已拥有道具范围。本批活动 323 只验证留存输入下的搜索回归，不另外验收线上 HHWX 活动 323 的完整参数。每例仍限时 300 秒、搜索存储 256 MiB、原生进程采样上限 1 GiB；不临时增加预算或改算法。

本批于 2026-09-01 在干净提交 `8c758bf` 上完成：**48 场同输入分数回归检查全部通过**。其中 45 场完成 exact：42 场与主要参考相同，3 场更高，分别为 1211 卡无活动（+193）、1211 卡活动 260（+155）、1252 卡活动 323（+154）。**1229 卡活动 244、1513 卡活动 244、1703 卡活动 260 在 300 秒超时**，但三场都已追平主要参考；它们只是尚未完成最优性证明的三场。没有低于主要参考的分数、进程失败或内存限制中止。

原生搜索合计 3,606.484 秒，各场耗时中位数 28.550 秒；采样进程峰值 40.36 MiB，预算内搜索存储峰值 32.16 MiB，全部场景均采到内存。已核对输入／文件哈希、保存输出一致性、队伍合法性及每曲整数分数之和，没有重新计分。逐场分数、耗时、内存、诊断计数和旧报告位置保存在 `runs/2026-08-31T15-20-54.142Z/`，包括 `report.md`、`run.json` 和 `summary.json`。

事后来源审计撤销了另外附加的五项比较。所谓 1229 卡四场实际来自同一账号后来增长到 1252 卡时的另一份档案，内容哈希不同；1513 卡活动 323 虽然档案内容相同，但使用了包含 HHWX 演出加成的另一套活动参数和未提交的实验搜索代码。它们都不是本批同输入参考。因此，本批要求的 **48 场搜索分数回归检查已经全部通过**；现在只剩三场最优性证明超时。本轮没有改计分／搜索、增加预算或复算旧队伍。

## 最终高压力阶段

剩余五份档案整体保留到最后：**1051、1127、1329、1747、1889 卡**。不把其中已经完成的个别场景拆进当前批次。本轮不执行高压力测试。

1051、1127、1747 卡未满足四场全 exact；1329／1889 卡的早期报告使用不同歌曲组合，且存在锁定道具或缺少历史 PERFECT 比例的情况，最终阶段开始前再确认其具体场景设置。锁定道具的 exact 或未完成时的暂时最高分，都不能算完整范围验收通过。
